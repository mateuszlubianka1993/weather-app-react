import React from 'react';

const SearchBar = () => {
    return (
        <div>
            <form>
                <input type="text" />
                <label>Search city </label>
            </form>
        </div>
    );
};

export default SearchBar;
